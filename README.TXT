you can test the plugin locally with using the it in src/it/gwt-compile
after clean install in the root of the mojo
cd src/it/gwt-compile and run any mojo you want with specifying the version with -DgwtPluginVersion=${gwtPluginVersion}
